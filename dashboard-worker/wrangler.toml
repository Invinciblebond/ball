name = "urlsifydash"
main = "worker.js"
compatibility_date = "2026-05-10"

# Same KV namespace the shortener uses. Two workers may bind the same
# namespace; this one only adds keys, it never changes how the
# shortener reads the ones it already owns.
[[kv_namespaces]]
binding = "LINKS"
id = "2aed9fa18b474213a970601aae8fafb7"

# More specific than urlsify-worker's route, so Cloudflare sends
# /api/dash/* here and leaves every other path to the shortener.
# Comment this block out to stay on the workers.dev subdomain instead.
[[routes]]
pattern = "urlsify.com/api/dash/*"
zone_name = "urlsify.com"

[vars]
SUPABASE_URL = "https://ytvyhkhzewnultjfpcyf.supabase.co"

[observability]
enabled = true

# Secrets, set in the dashboard under Variables and Secrets:
#   DISCORD_WEBHOOK           required — all notifications (new links,
#                             new accounts, deletions) go to this one.
#   SUPABASE_SERVICE_ROLE_KEY optional — lets a full-erasure request
#                             delete the Supabase auth record itself.
#                             Without it, erasures are flagged in the
#                             webhook for manual removal.
#   SUPABASE_JWT_SECRET       not needed — this project signs with
#                             ES256, verified via the public JWKS.
